---
import type { ComparisonRow } from '../data/comparisons';

interface Props { rows: ComparisonRow[]; competitor: string }
const { rows, competitor } = Astro.props;
---
<div class="table-scroll comparison-table">
  <table>
    <thead><tr><th>Criterion</th><th>AliasMode</th><th>{competitor}</th><th>Evidence</th></tr></thead>
    <tbody>
      {rows.map((row: ComparisonRow) => (
        <tr>
          <th scope="row">{row.criterion}</th>
          <td>{row.aliasMode}</td>
          <td>{row.competitor}</td>
          <td>{row.source ? <a href={row.source} rel="noreferrer">Official source</a> : 'AliasMode product record'}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

<style>
  .comparison-table { background: rgba(139, 167, 255, .03); border: 1px solid var(--line); border-radius: .8rem; margin: 2rem 0; }
  .comparison-table th[scope="row"] { background: rgba(139, 167, 255, .06); color: var(--ink); font-size: .88rem; letter-spacing: 0; text-transform: none; }
  .comparison-table td { color: var(--muted); min-width: 14rem; }
  .comparison-table td:first-of-type { color: var(--accent-bright); }
  .comparison-table a { color: var(--accent-bright); font-weight: 700; }
</style>
