# AliasMode Site Instructions

- For website-only edits, skip CI and test runs unless the user explicitly requests them.
- After completing website-only edits, commit and push directly to `origin/main`, then deploy production. This is standing authorization for this repository.
